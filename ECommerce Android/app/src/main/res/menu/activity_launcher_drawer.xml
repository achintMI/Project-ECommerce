<?xml version="1.0" encoding="utf-8"?>
<menu
   xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:showIn="navigation_view">
    <item android:visible="true" android:id="@+id/cat"
        android:title="Category"
        android:icon="@drawable/ic_menu_camera"
        android:enabled="true">
    </item>

    <item android:visible="true" android:id="@+id/cart"
        android:title="Cart"
        android:icon="@drawable/ic_menu_camera"
        android:enabled="true">
    </item>

    <item android:visible="true" android:id="@+id/order_history_act"
        android:title="Order History"
        android:icon="@drawable/ic_menu_camera"
        android:enabled="true">
    </item>

    <item android:visible="true" android:id="@+id/m_filte"
        android:title="Sort By"
        android:icon="@drawable/ic_menu_camera"
        android:enabled="true">
        <menu>
            <item android:id="@+id/price_filter" android:title="Price"></item>
            <item android:id="@+id/rating_filter" android:title="Rating"></item>
        </menu>
    </item>
</menu>
